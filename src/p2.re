/*
    Implement the following function
    solve : int -> 'a list -> 'a option
    It should return the element at the specified position. (easy)

    - Do not use the List module.
 */

let solve = (position, list) => (position, list);

Js.log(solve(3, ['a', 'b', 'c', 'd', 'e', 'f', 'g']));
