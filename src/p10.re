/*
    39. A list of prime numbers. (easy)
    Given a range of integers by its lower and upper limit, construct a list of all prime numbers in that range.
 */
open Data;

let solve = (lower, upper) => [];

printAnswer(solve(2, 666));
